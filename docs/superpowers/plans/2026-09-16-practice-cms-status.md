# 内容后台验收状态

## 已完成（本地）

- 六个模块的独立 Markdown 内容目录、统一校验、卡片与详情展示。
- 中文 Decap 编辑器、Markdown 导入、图片附件、预览、展示开关。
- 39 个旧条目的维护入口，保留旧页面地址与内容。
- 浏览器实际验证：模板导入、保存到项目、开启展示、关闭后详情不可访问、连续编辑保存状态。
- 本地代理仅绑定 127.0.0.1。测试使用真实工作目录的独立临时案例，验收后删除；不是隔离仓库。
- 55 个测试通过，生产构建通过。构建有主包体积提示，不影响生成。

## 已确认（线上配置）

- GitHub 仓库：`zichengwangai-1/muxin-zero-to-ai`。
- Netlify 生产分支：`main`；构建命令：`npm run build`；发布目录：`dist`。
- `site-settings.json` 的 `publishBranch` 已填写为 `main`。

## 仍待完成（线上）

- Netlify 已完成仓库关联，并已用 GitHub `main` 的当前版本成功构建；本次后台代码尚待提交并触发下一次构建。
- 配置 GitHub OAuth。
- 审查并发布所需改动，不能把当前其他未提交修改一并擅自上线。
- 实际验收线上登录、草稿、发布、自动部署和线上下架。

后台代码尚未提交 Git。当前在线模式有明确的未配置提示，不使用假分支或假登录。
